import ProfileClient from "./ProfileClient";

export const metadata = {
    title: "My Profile | Property Portal",
    description: "Manage your account settings and password.",
};

export default function Page() {
    return <ProfileClient />;
}
